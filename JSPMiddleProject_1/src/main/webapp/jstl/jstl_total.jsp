<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>
<%-- 
		EL => 출력
		   => ${request.getAttribute()} => 변수명이 아니고 키명을 쓴다.
		   		======================
		   	  ${session.getAttribute()}
		   	  
		JSTL => 제어문 / URL / 변환 / String메소드 호출
		==== core / format / fn
		==== JSP는 데이터베이스를 연동하는 곳이 아니라 전송받은 데이터만 출력하는 파일로 사용 (View)
		     보안문제 , 자바와 혼합 문제 (퍼블리셔) => 따라서 자바와 분리시킨다.
		     <c:forEach>
		     <c:forTokens>
		     <c:if>
		     <c:choose>
		     <c:set>
		     <c:out>
		     
		     <fmt:formatDate>
		     <fmt:formatNumber>
		     
		     ${fn:String 메소드} => 자바에서 제어 후 들어오기 때문에 많이 사용하지는 않는다.
--%>
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Insert title here</title>
</head>
<body>

</body>
</html>